import { getCoffees, getFace, getNaps, getSnack, getStudy } from './MoodSelector';

describe('mood selector testing', () => {
    it('true if coffee and naps are less than 1', () => {
        const state = {
            coffees: 0,
            snacks: 2,
            naps: 0,
            studies: 5
        };
    });
});